# Cloudflare Workers Environment Variables
# Copy this to .env and fill in your values
# DO NOT COMMIT .env TO GIT
# Pulumi passphrase for encrypting secrets
# Set this to avoid being prompted every time
PULUMI_CONFIG_PASSPHRASE=your-pulumi-passphrase

# =============================================================================
# Development/Production Mode
# =============================================================================
ENVIRONMENT=development  # or "production"

# =============================================================================
# Email Configuration (AWS SES)
# =============================================================================
EMAIL_FROM=noreply@yourdomain.com
FROM_NAME=Your App Name
AWS_REGION=us-east-1

# =============================================================================
# JWT Configuration
# Generate a secure secret: openssl rand -base64 32
# =============================================================================
JWT_SECRET=your-super-secret-jwt-key-here

# =============================================================================
# OAuth Provider Credentials (optional)
# =============================================================================
# GitHub OAuth
OAUTH_GITHUB_CLIENT_ID=
OAUTH_GITHUB_CLIENT_SECRET=

# Google OAuth
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_GOOGLE_CLIENT_SECRET=

# Twitter OAuth
OAUTH_TWITTER_CLIENT_ID=
OAUTH_TWITTER_CLIENT_SECRET=

# =============================================================================
# App URLs
# =============================================================================
APP_URL=http://localhost:5174
API_URL=http://localhost:8787

# =============================================================================
# Cloudflare Configuration (OPTIONAL - only needed for infrastructure deployment)
# =============================================================================
# These are only needed if running Pulumi commands from the root directory
# Usually you'll run Pulumi from the infrastructure/ directory instead
#
# Get from: https://dash.cloudflare.com/
# CF_ACCOUNT_ID=your-account-id-here

# Get from: https://dash.cloudflare.com/profile/api-tokens
# CF_API_TOKEN=your-api-token-here

# Optional: For custom domain routing
# CF_ZONE_ID=your-zone-id-here

# =============================================================================
# Notes
# =============================================================================
# - Do NOT commit this file to version control (.env is in .gitignore)
# - For local development only, these values override wrangler.toml [vars]
# 
# Secrets (set with wrangler secrets for production):
#   wrangler secret put JWT_SECRET
#   wrangler secret put AWS_ACCESS_KEY_ID      # From Pulumi output
#   wrangler secret put AWS_SECRET_ACCESS_KEY  # From Pulumi output
#   wrangler secret put OAUTH_GITHUB_CLIENT_SECRET
#   wrangler secret put OAUTH_GOOGLE_CLIENT_SECRET
#   wrangler secret put OAUTH_TWITTER_CLIENT_SECRET
#
# Get AWS credentials from infrastructure deployment:
#   cd infrastructure
#   pulumi stack output awsAccessKeyId
#   pulumi stack output awsSecretAccessKey --show-secrets

