# Pulumi Infrastructure Environment Variables
# Copy this to .env and fill in your values

# =============================================================================
# Pulumi Configuration
# =============================================================================
# Required: Passphrase to encrypt/decrypt Pulumi secrets
# Generate one: openssl rand -base64 24
PULUMI_CONFIG_PASSPHRASE=your-pulumi-passphrase-here

# =============================================================================
# AWS Credentials (OPTIONAL - usually not needed)
# =============================================================================
# Pulumi automatically uses credentials from:
#   1. ~/.aws/credentials (AWS CLI default)
#   2. ~/.aws/config
#   3. Environment variables (below)
#
# Only set these if you want to override AWS CLI credentials:
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_DEFAULT_REGION=us-east-1
# AWS_PROFILE=your-profile-name

# =============================================================================
# Notes
# =============================================================================
# - Do NOT commit this file to version control
# - PULUMI_CONFIG_PASSPHRASE is REQUIRED
# - AWS credentials are usually read from ~/.aws/credentials automatically
# - All team members need the same Pulumi passphrase to work with the same stack
# - Check your AWS CLI config with: aws configure list
