/**
 * Admin Dashboard Routes
 *
 * All routes require platform admin API key (type=secret, tenant=relish-platform)
 * Provides comprehensive tenant, API key, and metrics management
 */

export { createAdminRouter } from "./dashboard";
